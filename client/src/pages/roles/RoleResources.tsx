import { create, tsx } from '@dojo/framework/core/vdom';

export interface RoleResourcesProperties {
}

const factory = create().properties<RoleResourcesProperties>();

export default factory(function RoleResources({ properties }){
    const {  } = properties();
    return (
        <div></div>
    );
});
